// API client placeholder - will be implemented when connecting to backend
// For now, the app uses localStorage for authentication

export const apiClient = {
  // Placeholder for future API implementation
};

export default apiClient;